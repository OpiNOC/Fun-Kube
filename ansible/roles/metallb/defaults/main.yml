# defaults file for metallb
# Nota: metallb_version è risolto e passato da runner.py come extra_var.
# Questo valore è un fallback di emergenza e non dovrebbe mai essere usato.
---
metallb_version: "v0.14.9"
